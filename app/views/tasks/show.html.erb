<% provide(:title, @user.name) %>

<div class="container">
  <h1>タスク詳細</h1>
  <div class="row">
    <div class="col-md-10 col-md-offset-1">
      <table class="table table-border" id="table-task">
        <tbody>
          <tr>
            <th>ID</th>
            <td><%= @task.id %></td>
          </tr>
          <tr>
            <th>名称</th>
            <td><%= @task.title %></td>
          </tr>
          <tr>
            <th>詳細</th>
            <td><%= @task.content %></td>
          </tr>
          <tr>
            <th>作成日</th>
            <td><%= @task.created_at.strftime("%Y/%m/%d %H:%M") %></td>
          </tr>
          <tr>
            <th>更新日</th>
            <td><%= @task.updated_at.strftime("%Y/%m/%d %H:%M") %></td>
          </tr>
        </tbody>
      </table>
      <div>
        <%= link_to "編集", edit_user_task_path(@user), class: "btn btn-success" %>
        <%= link_to "削除", user_task_path(@user), method: :delete, data: { confirm: "削除してよろしいですか？"}, class: "btn btn-danger" %>
      </div>
    </div>
  </div>
</div>